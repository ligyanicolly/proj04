let compra = Number(prompt("Escreva o número:"));
let desconto = Number(compra-(compra*0.1))

if (compra >= 100) {
    alert (`Voce obteve desconto de 10%, seu valor ficou no total de R$) ${desconto}`)
}else{

}
